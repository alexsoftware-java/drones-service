spring:
  application:
    name: drones-dispatcher-service
  datasource:
    url: jdbc:h2:mem:drones
    username: sa
    password: pa$$word
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
logging:
  level:
    org.springframework.web: DEBUG
    com.musala.interview: DEBUG
    org.hibernate: INFO
  file:
    name: ./logs/drones-dispatcher-service.log